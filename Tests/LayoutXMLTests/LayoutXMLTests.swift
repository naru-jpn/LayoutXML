import XCTest
@testable import LayoutXML

class LayoutXMLTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(LayoutXML().text, "Hello, World!")
    }


    static var allTests = [
        ("testExample", testExample),
    ]
}
